/// The request content type.
public enum RequestContentType {
    /// Indicates that the request contains JSON content.
    case json
    /// Indicates that the request contains URL encoded content.
    case urlEncoded
}
